---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cloudfoundry/cf-routing-pipeline

inputs:
  - name: routing-release
  - name: deployments-routing

outputs:
  - name: routing-manifest

params:
  ENVIRONMENT: ~
  BOSH_TARGET: ~
  BOSH_USER: ~
  BOSH_PASSWORD: ~
  INFRASTRUCTURE: ~

run:
  path: deployments-routing/routing-ci/scripts/ci/create_routing_manifest/task
