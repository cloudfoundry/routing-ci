---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cloudfoundry/cf-routing-pipeline

inputs:
- name: deployments-routing
- name: routing-release

outputs:
  - name: perf-manifest

run:
  path: deployments-routing/routing-ci/scripts/ci/generate-perf-manifest/task

params:
  ENV_DIR: deployments-routing/diana
  ROUTING_RELEASE_DIR: routing-release
  OUTPUT_DIR: perf-manifest
