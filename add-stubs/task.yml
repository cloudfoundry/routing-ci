---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cloudfoundry/cf-routing-pipeline

inputs:
  - name: deployments-routing
  - name: routing-ci
  - name: modified-deployments-routing

outputs:
  - name: env-repo

run:
  path: routing-ci/add-stubs/task

params:
  ENVIRONMENT:
  ADMIN_PASSWORD:
  ADMIN_CLIENT_SECRET:
  GIT_COMMIT_EMAIL:
