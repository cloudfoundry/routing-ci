---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cloudfoundry/cf-routing-pipeline

inputs:
  - name: deployments-routing

run:
  path: deployments-routing/routing-ci/scripts/ci/emit_datadog_event

params:
  TITLE:
  TEXT:
  PRIORITY:
  AWS_ENV:
  DATADOG_API_KEY:
