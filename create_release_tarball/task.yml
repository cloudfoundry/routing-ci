---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cloudfoundry/cf-routing-pipeline

inputs:
  - name: release-dir
  - name: deployments-routing

outputs:
  - name: release-tarball

run:
  path: deployments-routing/routing-ci/scripts/ci/create_release_tarball/task

params:
  RELEASE_DIR: release-dir
