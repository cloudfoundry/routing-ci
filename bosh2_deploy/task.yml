---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: relintdockerhubpushbot/cf-deployment-concourse-tasks

inputs:
  - name: deployments-routing
  - name: gcp-stemcell
  - name: cf-release-bosh-tarball
  - name: release-tarball
  - name: routing-ci

run:
  path: routing-ci/bosh2_deploy/task

params:
  MANIFEST:
  ENVIRONMENT:
  DEPLOYMENT:
  RELEASES_DIR:
  STEMCELL_REGEX:
