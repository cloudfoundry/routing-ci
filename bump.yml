---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cloudfoundry/cf-routing-pipeline
    tag: bbl3

inputs:
  - name: cf-release-dev
  - name: deployments-routing
  - name: routing-release
  - name: routing-ci

outputs:
  - name: bumped

run:
  path: routing-ci/bump
