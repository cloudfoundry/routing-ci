---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cloudfoundry/cf-routing-pipeline

inputs:
  - name: cf-release-dev
  - name: deployments-routing
  - name: routing-release

outputs:
  - name: bumped

run:
  path: deployments-routing/routing-ci/scripts/ci/bump
