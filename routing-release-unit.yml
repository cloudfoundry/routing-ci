---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cloudfoundry/cf-routing-pipeline

inputs:
  - name: routing-release
  - name: deployments-routing
  - name: old-routing-release

params:
  PACKAGE:

run:
  path: deployments-routing/routing-ci/scripts/ci/run_routing_release_unit
