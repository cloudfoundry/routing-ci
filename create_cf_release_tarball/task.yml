---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cloudfoundry/cf-routing-pipeline

inputs:
  - name: deployments-routing
  - name: cf-release-source
  - name: version

outputs:
  - name: cf-release-tarball

run:
  path: deployments-routing/routing-ci/scripts/ci/create_cf_release_tarball/task

params:
  CF_RELEASE_DIR: cf-release-source
