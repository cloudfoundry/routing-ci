---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cloudfoundry/cf-routing-pipeline

inputs:
  - name: deployments-routing
  - name: zero-downtime

run:
  path: deployments-routing/routing-ci/scripts/ci/deploy-http-app/task

params:
  ENVIRONMENT:
