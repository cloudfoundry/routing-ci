---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cloudfoundry/cf-routing-pipeline

inputs:
  - name: cf-deployment
  - name: deployments-routing

outputs:
  - name: modified-cf-deployment

params:
  ENVIRONMENT:

run:
  path: deployments-routing/routing-ci/scripts/ci/copy_files_to_operations/task
