---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cloudfoundry/cf-routing-pipeline

inputs:
- name: cf-release-source
- name: deployments-routing
- name: routing-ci

outputs:
- name: cf-manifest

run:
  path: routing-ci/create_cf_release_manifest

params:
  INFRASTRUCTURE:
  ENVIRONMENT:
